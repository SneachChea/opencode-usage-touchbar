name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v6

      - name: Show toolchain
        run: |
          swift --version
          xcodebuild -version

      - name: Run tests
        run: swift test

      - name: Build universal release
        run: ./scripts/release.sh

      - name: Inspect application binary
        run: |
          file "dist/OpenCode Usage TouchBar.app/Contents/MacOS/opencode-usage-touchbar"
          lipo -archs "dist/OpenCode Usage TouchBar.app/Contents/MacOS/opencode-usage-touchbar"
          cd dist
          shasum -a 256 -c opencode-usage-touchbar-v*.zip.sha256

      - name: Upload build artifact
        uses: actions/upload-artifact@v6
        with:
          name: opencode-usage-touchbar
          path: |
            dist/opencode-usage-touchbar-v*.zip
            dist/opencode-usage-touchbar-v*.zip.sha256

      - name: Publish tagged release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          info_version="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' Info.plist)"
          test "$version" = "$info_version"
          gh release create "$GITHUB_REF_NAME" \
            dist/opencode-usage-touchbar-v*.zip \
            dist/opencode-usage-touchbar-v*.zip.sha256 \
            --generate-notes \
            --title "OpenCode Usage TouchBar $GITHUB_REF_NAME"
